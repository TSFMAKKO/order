<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        :root {
            /* 蘋果黑灰白配色 */
            --primary-black: #1D1D1F;
            --secondary-black: #2D2D2D;
            --primary-gray: #86868B;
            --secondary-gray: #424245;
            --light-gray: #F5F5F7;
            --white: #FFFFFF;
            
            /* 強調色 */
            --accent-blue: #2997FF;
            --hover-blue: #0071E3;
            
            /* 背景色 */
            --background-primary: var(--white);
            --background-secondary: var(--light-gray);
            --background-elevated: var(--white);
            
            /* 文字顏色 */
            --text-primary: var(--primary-black);
            --text-secondary: var(--primary-gray);
            
            /* 邊框和分隔線 */
            --separator-color: #D2D2D7;
            
            /* 特殊效果 */
            --backdrop-blur: saturate(180%) blur(20px);
        }

        body {
            font-family: "SF Pro TC","SF Pro Text","SF Pro Icons","PingFang TC","Helvetica Neue","Helvetica","Arial",sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--background-primary);
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            color: var(--text-primary);
        }

        .nav-wrapper {
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: var(--backdrop-blur);
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 9999;
            border-bottom: 1px solid var(--separator-color);
        }

        .navbar {
            max-width: 980px;
            margin: 0 auto;
            padding: 0 22px;
            height: 48px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .nav-brand {
            font-size: 21px;
            font-weight: 600;
            color: var(--text-primary);
            text-decoration: none;
        }

        .nav-links {
            display: flex;
            gap: 30px;
            align-items: center;
        }

        .nav-link {
            color: var(--text-secondary);
            text-decoration: none;
            font-size: 14px;
            transition: color 0.3s ease;
        }

        .nav-link:hover {
            color: var(--accent-blue);
        }

        .hero-section {
            background: var(--primary-black);
            color: var(--white);
            text-align: center;
            padding: 120px 20px 60px;
            margin-bottom: 40px;
        }

        .hero-title {
            font-size: 48px;
            font-weight: 600;
            margin-bottom: 16px;
            letter-spacing: -0.003em;
        }

        .hero-subtitle {
            font-size: 24px;
            font-weight: 400;
            margin-bottom: 32px;
            letter-spacing: .011em;
            color: var(--primary-gray);
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .cta-button {
            display: inline-block;
            background: var(--accent-blue);
            color: var(--white);
            padding: 12px 32px;
            border-radius: 980px;
            text-decoration: none;
            font-size: 17px;
            font-weight: 400;
            transition: all 0.3s ease;
        }

        .cta-button:hover {
            background-color: var(--hover-blue);
            transform: translateY(-1px);
        }

        .main-content {
            max-width: 980px;
            margin: 0 auto;
            padding: 0 20px 40px;
        }

        .categories-nav {
            display: flex;
            gap: 12px;
            margin-bottom: 32px;
            flex-wrap: wrap;
        }

        .category-link {
            color: var(--text-secondary);
            text-decoration: none;
            padding: 6px 16px;
            border-radius: 980px;
            font-size: 14px;
            transition: all 0.3s ease;
            background: var(--background-secondary);
        }

        .category-link:hover,
        .category-link.active {
            background: var(--primary-black);
            color: var(--white);
        }

        .section-title {
            font-size: 28px;
            font-weight: 600;
            color: var(--text-primary);
            margin: 0 0 32px;
            letter-spacing: .007em;
        }

        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 30px;
            margin-bottom: 40px;
        }

        .product-card {
            background: var(--background-elevated);
            border-radius: 18px;
            overflow: hidden;
            transition: all 0.3s ease;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        .product-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.12);
        }

        .product-image-container {
            position: relative;
            padding-top: 75%;
            background: var(--background-secondary);
            overflow: hidden;
        }

        .product-image {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        .product-card:hover .product-image {
            transform: scale(1.05);
        }

        .product-info {
            padding: 24px;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
        }

        .product-name {
            font-size: 17px;
            font-weight: 600;
            color: var(--text-primary);
            margin: 0 0 8px;
            line-height: 1.3;
        }

        .product-description {
            font-size: 14px;
            color: var(--text-secondary);
            margin: 0 0 16px;
            line-height: 1.4;
            flex-grow: 1;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .product-price {
            font-size: 17px;
            font-weight: 600;
            color: var(--text-primary);
            margin: 0 0 16px;
        }

        .product-actions {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 12px;
        }

        .view-details {
            color: var(--accent-blue);
            text-decoration: none;
            font-size: 14px;
            padding: 8px 0;
        }

        .add-to-cart {
            background: var(--accent-blue);
            color: var(--white);
            border: none;
            padding: 8px 20px;
            border-radius: 980px;
            font-size: 14px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .add-to-cart:hover {
            background-color: var(--hover-blue);
        }

        @media (prefers-color-scheme: dark) {
            :root {
                --background-primary: var(--primary-black);
                --background-secondary: var(--secondary-black);
                --background-elevated: var(--secondary-black);
                --text-primary: var(--white);
                --text-secondary: var(--primary-gray);
                --separator-color: var(--secondary-gray);
            }

            .nav-wrapper {
                background: rgba(29, 29, 31, 0.8);
            }

            .hero-section {
                background: var(--secondary-black);
            }

            .product-card {
                box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
            }

            .product-card:hover {
                box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
            }
        }

        @media (max-width: 734px) {
            .hero-title {
                font-size: 32px;
            }

            .hero-subtitle {
                font-size: 19px;
            }

            .section-title {
                font-size: 24px;
                margin-bottom: 24px;
            }

            .products-grid {
                grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
                gap: 20px;
            }

            .product-info {
                padding: 20px;
            }

            .nav-links {
                gap: 20px;
            }

            .categories-nav {
                gap: 8px;
                margin-bottom: 24px;
            }

            .category-link {
                padding: 4px 12px;
                font-size: 13px;
            }
        }
    </style>
</head>
<body>
    <div class="nav-wrapper">
        <nav class="navbar">
            <a href="/" class="nav-brand">YuChin購物網站</a>
            <div class="nav-links">
                <a href="/" class="nav-link">首頁</a>
                <a href="/products" class="nav-link">商品列表</a>
                <a href="/cart" class="nav-link">購物車</a>
            </div>
        </nav>
    </div>

    <section class="hero-section">
        <h1 class="hero-title">探索優質商品</h1>
        <p class="hero-subtitle">我們精選多樣化的商品，讓您安心購物、輕鬆付款</p>
        <a href="#products" class="cta-button">立即選購</a>
    </section>

    <main class="main-content">
        <nav class="categories-nav">
            <a href="/" class="category-link active">全部商品</a>
            <% categories.forEach(function(category) { %>
                <a href="/products?category=<%= category %>" class="category-link">
                    <%= category === 'electronics' ? '電子產品' : 
                       category === 'fashion' ? '時尚服飾' : 
                       category %>
                </a>
            <% }); %>
        </nav>

        <h2 class="section-title">熱門商品</h2>
        <div class="products-grid">
            <% products.forEach(function(product) { %>
                <div class="product-card">
                    <div class="product-image-container">
                        <img src="<%= product.image %>" alt="<%= product.name %>" class="product-image" onerror="this.src='/img/no-image.png'">
                    </div>
                    <div class="product-info">
                        <h3 class="product-name"><%= product.name %></h3>
                        <p class="product-description"><%= product.description %></p>
                        <div class="product-price">NT$ <%= product.price.toLocaleString() %></div>
                        <div class="product-actions">
                            <a href="/products/<%= product.id %>" class="view-details">查看詳情</a>
                            <form action="/cart/add" method="POST" style="margin: 0;">
                                <input type="hidden" name="productId" value="<%= product.id %>">
                                <input type="hidden" name="name" value="<%= product.name %>">
                                <input type="hidden" name="price" value="<%= product.price %>">
                                <input type="hidden" name="image" value="<%= product.image %>">
                                <input type="hidden" name="quantity" value="1">
                                <button type="submit" class="add-to-cart">加入購物車</button>
                            </form>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </main>

    <footer class="footer">
        <div class="footer-content">
            <div class="footer-section">
                <h4 class="footer-title">關於我們</h4>
                <ul class="footer-links">
                    <li><a href="/about">公司介紹</a></li>
                    <li><a href="/contact">聯絡我們</a></li>
                    <li><a href="/privacy">隱私政策</a></li>
                    <li><a href="/terms">服務條款</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h4 class="footer-title">購物指南</h4>
                <ul class="footer-links">
                    <li><a href="/guide">購物流程</a></li>
                    <li><a href="/payment">付款方式</a></li>
                    <li><a href="/shipping">運送方式</a></li>
                    <li><a href="/faq">常見問題</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h4 class="footer-title">會員中心</h4>
                <ul class="footer-links">
                    <li><a href="/account">會員資料</a></li>
                    <li><a href="/orders">訂單查詢</a></li>
                    <li><a href="/wishlist">收藏清單</a></li>
                    <li><a href="/newsletter">電子報訂閱</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h4 class="footer-title">追蹤我們</h4>
                <ul class="footer-links">
                    <li><a href="https://facebook.com">Facebook</a></li>
                    <li><a href="https://instagram.com">Instagram</a></li>
                    <li><a href="https://line.me">Line</a></li>
                    <li><a href="https://youtube.com">YouTube</a></li>
                </ul>
            </div>
        </div>
        <div class="footer-bottom">
            <p class="copyright">© 2024 YuChin購物網站. All rights reserved.</p>
            <p class="powered-by">Powered by <a href="https://www.ecpay.com.tw/" target="_blank" rel="noopener">ECPay</a></p>
        </div>
    </footer>

    <style>
        .footer {
            background-color: var(--background-secondary);
            padding: 60px 0 20px;
            margin-top: 80px;
            color: var(--text-secondary);
        }

        .footer-content {
            max-width: 980px;
            margin: 0 auto;
            padding: 0 22px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 40px;
        }

        .footer-title {
            color: var(--text-primary);
            font-size: 14px;
            font-weight: 600;
            margin: 0 0 16px;
            letter-spacing: 0.007em;
        }

        .footer-links {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .footer-links li {
            margin-bottom: 12px;
        }

        .footer-links a {
            color: var(--text-secondary);
            text-decoration: none;
            font-size: 13px;
            transition: color 0.3s ease;
        }

        .footer-links a:hover {
            color: var(--accent-blue);
        }

        .footer-bottom {
            max-width: 980px;
            margin: 40px auto 0;
            padding: 20px 22px 0;
            border-top: 1px solid var(--separator-color);
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 10px;
        }

        .copyright,
        .powered-by {
            font-size: 12px;
            color: var(--text-secondary);
            margin: 0;
        }

        .powered-by a {
            color: var(--accent-blue);
            text-decoration: none;
        }

        .powered-by a:hover {
            text-decoration: underline;
        }

        @media (max-width: 734px) {
            .footer {
                padding: 40px 0 20px;
                margin-top: 60px;
            }

            .footer-content {
                grid-template-columns: repeat(2, 1fr);
                gap: 30px;
            }

            .footer-bottom {
                margin-top: 30px;
                justify-content: center;
                text-align: center;
            }

            .copyright,
            .powered-by {
                width: 100%;
            }
        }

        @media (prefers-color-scheme: dark) {
            .footer {
                background-color: var(--background-elevated);
            }
        }
    </style>
</body>
</html> 